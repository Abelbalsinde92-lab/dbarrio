const UPLOAD_CONFIG={image:{maxSizeMB:1,maxOriginalMB:10,quality:.75,maxWidthPx:1200},pdf:{maxSizeMB:2}};async function compressImage(file){const cfg=UPLOAD_CONFIG.image;if(!file.type.startsWith("image/"))return{blob:null,sizeMB:0,error:"El archivo debe ser una imagen (JPG, PNG, WEBP)."};const originalMB=file.size/(1024*1024);if(originalMB>cfg.maxOriginalMB)return{blob:null,sizeMB:0,error:`La imagen supera los ${cfg.maxOriginalMB} MB. Usa una foto más pequeña.`};return new Promise((resolve)=>{const img=new Image();const url=URL.createObjectURL(file);img.onload=()=>{URL.revokeObjectURL(url);let{width,height}=img;if(width>cfg.maxWidthPx){height=Math.round((height*cfg.maxWidthPx)/width);width=cfg.maxWidthPx;}const canvas=document.createElement("canvas");canvas.width=width;canvas.height=height;const ctx=canvas.getContext("2d");ctx.drawImage(img,0,0,width,height);canvas.toBlob((blob)=>{if(!blob){resolve({blob:null,sizeMB:0,error:"No se pudo procesar la imagen."});return;}const sizeMB=blob.size/(1024*1024);if(sizeMB>cfg.maxSizeMB){resolve({blob:null,sizeMB,error:`La imagen sigue siendo demasiado grande (${sizeMB.toFixed(1)} MB). Usa una foto con menos resolución.`});return;}resolve({blob,sizeMB,error:null});},"image/jpeg",cfg.quality);};img.onerror=()=>{URL.revokeObjectURL(url);resolve({blob:null,sizeMB:0,error:"No se pudo leer la imagen."});};img.src=url;});}function validatePDF(file){if(file.type!=="application/pdf")return{valid:false,error:"El documento debe ser un archivo PDF."};const sizeMB=file.size/(1024*1024);if(sizeMB>UPLOAD_CONFIG.pdf.maxSizeMB)return{valid:false,error:`El PDF supera los ${UPLOAD_CONFIG.pdf.maxSizeMB} MB. Comprime el documento antes de subirlo.`};return{valid:true,error:null};}async function uploadToSupabase(supabase,fileOrBlob,bucket,path){const{data,error}=await supabase.storage.from(bucket).upload(path,fileOrBlob,{upsert:true,contentType:fileOrBlob.type||"application/octet-stream"});if(error){console.error("Supabase upload error:",error);return{url:null,error:"Error al subir el archivo. Intenta de nuevo."};}const{data:urlData}=supabase.storage.from(bucket).getPublicUrl(path);return{url:urlData.publicUrl,error:null};}async function processAndUpload(supabase,file,bucket,path,onProgress=()=>{}){if(!file)return{url:null,error:"No se seleccionó ningún archivo."};if(file.type.startsWith("image/")){onProgress("Procesando imagen...");const{blob,error}=await compressImage(file);if(error)return{url:null,error};onProgress("Subiendo imagen...");return await uploadToSupabase(supabase,blob,bucket,path);}if(file.type==="application/pdf"){const{valid,error}=validatePDF(file);if(!valid)return{url:null,error};onProgress("Subiendo documento...");return await uploadToSupabase(supabase,file,bucket,path);}return{url:null,error:"Tipo de archivo no permitido. Solo imágenes (JPG, PNG) o PDF."};}if(typeof module!=="undefined"){module.exports={processAndUpload,compressImage,validatePDF,uploadToSupabase};}else{window.dbUpload={processAndUpload,compressImage,validatePDF,uploadToSupabase};}
